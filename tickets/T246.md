# T246

## GOAL
Hook audio feedback stub into action flow.

## CHANGES
Ensure AudioManager script exists and action path calls UI click stub.

## ACCEPTANCE
- AudioManager present
- PetController UseAction calls PlayUiClick

## ACTIONS
WRITE Assets/Scripts/AudioManager.cs <<<EOF
using UnityEngine;

public class AudioManager : MonoBehaviour
{
    public static AudioManager Instance;

    private void Awake()
    {
        if (Instance != null && Instance != this)
        {
            Destroy(gameObject);
            return;
        }
        Instance = this;
    }

    public void PlayUiClick() { }
    public void PlayActionFeed() { }
    public void PlayActionPlay() { }
    public void PlayActionSleep() { }
    public void PlayWarning() { }
}
EOF
REPLACE Assets/Scripts/PetController.cs PATTERN
<<<EOF
        ApplyDelta(dh, dm, de, label);
EOF
REPLACEMENT <<<EOF
        AudioManager.Instance?.PlayUiClick();
        ApplyDelta(dh, dm, de, label);
EOF
